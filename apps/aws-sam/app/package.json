{
  "name": "aws-sam",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "cd .. && sam local start-api --port 3001 --env-vars env.local.json",
    "unit": "jest",
    "compile": "tsc",
    "test": "npm run compile && npm run unit"
  },
  "dependencies": {
    "esbuild": "^0.14.14",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "@jest/globals": "^29.2.0",
    "@types/aws-lambda": "^8.10.92",
    "@types/jest": "^29.2.0",
    "@types/node": "^20.5.7",
    "@workspace/eslint-config": "file:../../../packages/eslint-config",
    "@workspace/typescript-config": "file:../../../packages/typescript-config",
    "jest": "^29.2.1",
    "ts-jest": "^29.0.5",
    "ts-node": "^10.9.1"
  }
}
